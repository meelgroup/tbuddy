# Compute paths
get_filename_component(TBUDDY_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(TBUDDY_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
include("${TBUDDY_CMAKE_DIR}/@TBUDDY_TARGETS_FILENAME@")

# These are IMPORTED targets created by @TBUDDY_TARGETS_FILENAME@
set(TBUDDY_LIBRARIES tbuddy)
set(TBUDDY_STATIC_LIBRARIES tbuddy)
set(TBUDDY_STATIC_LIBRARIES_DEPS @TBUDDY_STATIC_DEPS@)
set(TBUDDY_VERSION_MAJOR @PROJECT_VERSION_MAJOR@)
set(TBUDDY_VERSION_MINOR @PROJECT_VERSION_MINOR@)
